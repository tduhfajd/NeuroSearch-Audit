---
phase: 1
plan: 3
title: "Local Infrastructure Runbook and Foundation Verification"
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - README.md
  - docker-compose.yml
  - tests/test_health_contract.py
  - tests/test_foundation_smoke.py
autonomous: true
---

# Phase 1, Plan 3: Local Infrastructure Runbook and Foundation Verification

## Objective

Закрыть операционную часть Foundation: зафиксировать runbook запуска локальной БД (brew primary, Docker fallback) и добавить smoke-проверки, подтверждающие критерии фазы end-to-end.

## must_haves

1. Локальный путь поднятия PostgreSQL задокументирован и воспроизводим.
2. Foundation success criteria подтверждены smoke-верификацией (`uvicorn`, `/health`, migration).

## Tasks

<task type="auto">
  <name>Подготовить runbook локальной инфраструктуры</name>
  <files>README.md, docker-compose.yml</files>
  <action>
    Обновить инструкции запуска: основной путь через Homebrew (`postgresql@16`), fallback через Docker Compose.
    Добавить пошаговые команды: старт БД, применение Alembic, запуск Uvicorn, базовая проверка API.
  </action>
  <verify>rg -n "postgresql@16|docker compose|alembic|uvicorn" README.md</verify>
  <done>README содержит полный воспроизводимый сценарий локального запуска Foundation.</done>
</task>

<task type="auto">
  <name>Добавить smoke-тесты для health-контрактов и startup wiring</name>
  <files>tests/test_health_contract.py, tests/test_foundation_smoke.py</files>
  <action>
    Реализовать тесты, которые проверяют контракт `/health`, доступность `/health/db` и корректный импорт/инициализацию приложения.
    Тесты должны быть изолированы и не требовать внешних API.
  </action>
  <verify>pytest tests/test_health_contract.py tests/test_foundation_smoke.py -x -q</verify>
  <done>Smoke-тесты стабильно проходят и фиксируют базовое поведение Foundation.</done>
</task>

<task type="human-verify">
  <name>Ручная финальная проверка Success Criteria Phase 1</name>
  <files>README.md, backend/main.py, backend/db/migrations/versions/xxxx_initial_foundation.py</files>
  <action>
    По runbook выполнить полный цикл: поднять БД, применить миграцию, запустить сервер, проверить `/health`.
    Зафиксировать evidence для 4 критериев успеха фазы.
  </action>
  <verify>curl -s http://127.0.0.1:8000/health && alembic -c backend/db/migrations/alembic.ini current</verify>
  <done>Все 4 Success Criteria Phase 1 подтверждены, фаза готова к переходу в Phase 2.</done>
</task>

## Verification

После завершения всех задач:
- [ ] `pytest tests/ -x -q` — тесты зелёные
- [ ] `ruff check backend/ tests/ && ruff format --check backend/ tests/` — нет ошибок
- [ ] must_haves выполнены (evidence выше)

## Notes

План завершает Phase 1 без выхода в реализацию следующих фаз (Crawler/Analyzer/AI Bridge).
