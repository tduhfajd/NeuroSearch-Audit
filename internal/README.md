# internal

Go implementation packages for the deterministic core.

- `audit/`: audit lifecycle and package metadata.
- `crawl/`: crawl execution and URL discovery.
- `platform/`: config, logging, metrics, shared runtime concerns.
- `storage/`: persistence adapters.
